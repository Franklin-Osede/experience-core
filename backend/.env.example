# Experience Core Backend - Environment Variables Template
# Copy this file to .env and update with your values

# ============================================
# Application Configuration
# ============================================
NODE_ENV=development
PORT=5555
CORS_ORIGIN=*

# ============================================
# TypeORM Configuration
# ============================================
# Set to 'false' to use in-memory repositories (for testing)
# Set to 'true' to use PostgreSQL database
USE_TYPEORM=true

# ============================================
# Database Configuration (Required if USE_TYPEORM=true)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=experience_core
DB_SYNCHRONIZE=false
DB_LOGGING=false
DB_MIGRATIONS_RUN=false

# ============================================
# JWT Configuration (REQUIRED)
# ============================================
# IMPORTANT: Use a secure random string of at least 32 characters
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-secret-key-minimum-32-characters-long-change-in-production
JWT_EXPIRES_IN=7d

# ============================================
# Notes
# ============================================
# - Never commit the .env file to version control
# - In production, use secure secrets management
# - JWT_SECRET must be at least 32 characters long
# - When USE_TYPEORM=false, DB_* variables are optional
